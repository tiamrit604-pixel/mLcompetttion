# =============================================================================
# Bolted-Flange Looseness Detection -- Streamlit App Requirements
# =============================================================================
# Tested with Python 3.10 - 3.12.
# Install with:  pip install -r requirements.txt
#
# IMPORTANT: ffmpeg is also required at the SYSTEM level (not via pip) for
# decoding .m4a files. See README.md for OS-specific install instructions.
# =============================================================================

# --- Streamlit + audio I/O ---------------------------------------------------
# Streamlit >= 1.31 is required for native st.audio_input (live mic in browser).
# If you cannot upgrade Streamlit, the app falls back to streamlit-mic-recorder
# automatically -- which is why it is also listed below.
streamlit>=1.32.0
streamlit-mic-recorder>=0.0.8
audioread>=3.0.0

# --- Numerical & ML stack ----------------------------------------------------
numpy>=1.24.0,<2.0
scipy>=1.10.0
scikit-learn>=1.3.0
pandas>=2.0.0
joblib>=1.3.0

# --- Audio feature extraction ------------------------------------------------
librosa>=0.10.0
soundfile>=0.12.0

# --- Deep learning -----------------------------------------------------------
# CPU build is fine; for GPU follow the official torch installation guide
# (https://pytorch.org/get-started/locally/) instead of using this line.
torch>=2.0.0

# --- Boosting ----------------------------------------------------------------
xgboost>=2.0.0

# --- Plotting ----------------------------------------------------------------
matplotlib>=3.7.0
seaborn>=0.12.0
